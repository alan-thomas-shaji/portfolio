---
/**
 * Navigation Component - Workshop Toggle Switches
 * 
 * Navigation links styled as physical toggle switches or wired-in buttons
 * with a workshop aesthetic.
 * - Hides when at top of page, shows on scroll
 * - Mobile: Hamburger menu with side drawer
 */

interface Props {
  currentSection?: string;
}

const { currentSection = '' } = Astro.props;

const navItems = [
  { id: 'hero', label: 'About', href: '#hero' },
  { id: 'experience', label: 'Experience', href: '#experience' },
  { id: 'projects', label: 'Projects', href: '#projects' },
  { id: 'contact', label: 'Contact', href: '#contact' },
];
---

<nav id="main-nav" class="nav-bar nav-hero fixed top-0 left-0 right-0 z-50 bg-[#1a1612]/95 backdrop-blur-sm transition-all duration-300">
  <div class="container mx-auto px-4 py-3">
    <div class="flex items-center justify-between">
      <a href="#hero" class="nav-brand font-bold text-xl transition-colors">
        <span class="font-mono">[ALAN THOMAS SHAJI]</span>
      </a>
      
      {/* Desktop Navigation */}
      <div class="desktop-nav flex gap-4 items-center">
        {navItems.map((item) => (
          <a
            href={item.href}
            class={`nav-toggle ${currentSection === item.id ? 'active' : ''}`}
            data-section={item.id}
          >
            <span class="toggle-label">{item.label}</span>
            <div class="toggle-switch">
              <div class="toggle-indicator"></div>
            </div>
          </a>
        ))}
      </div>

      {/* Mobile Hamburger Button */}
      <button 
        id="mobile-menu-toggle" 
        class="mobile-menu-btn"
        aria-label="Toggle menu"
        aria-expanded="false"
      >
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
      </button>
    </div>
  </div>
</nav>

{/* Mobile Side Menu */}
<div id="mobile-menu" class="mobile-menu">
  <div class="mobile-menu-overlay"></div>
  <div class="mobile-menu-drawer">
    <div class="mobile-menu-header">
      <span class="mobile-menu-title font-mono">[NAVIGATION]</span>
      <button 
        id="mobile-menu-close" 
        class="mobile-menu-close"
        aria-label="Close menu"
      >
        <span class="close-icon">×</span>
      </button>
    </div>
    <div class="mobile-menu-content">
      {navItems.map((item) => (
        <a
          href={item.href}
          class={`mobile-nav-item ${currentSection === item.id ? 'active' : ''}`}
          data-section={item.id}
        >
          <span class="mobile-nav-label">{item.label}</span>
          <div class="mobile-toggle-switch">
            <div class="mobile-toggle-indicator"></div>
          </div>
        </a>
      ))}
    </div>
  </div>
</div>

<style>
  /* Nav Bar Hide/Show on Scroll */
  .nav-bar {
    transform: translateY(0);
    position: fixed;
    transition: transform 0.5s cubic-bezier(0.4, 0.0, 0.2, 1);
  }

  .nav-bar.hidden {
    transform: translateY(-100%);
    transition: transform 0.3s ease-in;
  }

  /* Section-based color themes */
  .nav-bar {
    border-bottom: 2px solid;
    transition: border-color 0.4s ease, color 0.4s ease;
  }

  /* Hero Section - Gold/Orange */
  .nav-hero {
    border-color: rgba(255, 140, 0, 0.3);
  }

  .nav-hero .nav-brand {
    color: #ff8c00;
  }

  .nav-hero .nav-brand:hover {
    color: #ffa500;
  }

  .nav-hero .nav-toggle {
    color: #d4a574;
  }

  .nav-hero .nav-toggle:hover {
    color: #ff8c00;
  }

  .nav-hero .nav-toggle.active {
    color: #ff8c00;
  }

  .nav-hero .nav-toggle.active .toggle-indicator {
    left: calc(100% - 0.875rem - 0.1875rem);
    background: #ff8c00;
    box-shadow: 0 0 8px rgba(255, 140, 0, 0.8);
  }

  /* Experience Section - Blue Toggle */
  .nav-experience .toggle-switch {
    background: rgba(10, 10, 26, 0.8);
    border: 1px solid rgba(74, 158, 255, 0.4);
  }

  .nav-experience .toggle-indicator {
    background: rgba(74, 158, 255, 0.6);
  }

  .nav-hero .hamburger-line {
    background: #ff8c00;
  }

  .nav-hero .mobile-menu-title {
    color: #ff8c00;
  }

  .nav-hero .mobile-menu-drawer {
    border-left-color: rgba(255, 140, 0, 0.3);
  }

  /* Experience Section - Blue */
  .nav-experience {
    border-color: rgba(74, 158, 255, 0.3);
  }

  .nav-experience .nav-brand {
    color: #4a9eff;
  }

  .nav-experience .nav-brand:hover {
    color: #6bb0ff;
  }

  .nav-experience .nav-toggle {
    color: rgba(74, 158, 255, 0.7);
  }

  .nav-experience .nav-toggle:hover {
    color: #4a9eff;
  }

  .nav-experience .nav-toggle.active {
    color: #4a9eff;
  }

  .nav-experience .nav-toggle.active .toggle-indicator {
    left: calc(100% - 0.875rem - 0.1875rem);
    background: #4a9eff;
    box-shadow: 0 0 8px rgba(74, 158, 255, 0.8);
  }

  /* Projects Section - Red Toggle */
  .nav-projects .toggle-switch {
    background: rgba(42, 23, 23, 0.8);
    border: 1px solid rgba(122, 43, 43, 0.5);
  }

  .nav-projects .toggle-indicator {
    background: rgba(226, 85, 85, 0.6);
  }

  .nav-experience .hamburger-line {
    background: #4a9eff;
  }

  .nav-experience .mobile-menu-title {
    color: #4a9eff;
  }

  .nav-experience .mobile-menu-drawer {
    border-left-color: rgba(74, 158, 255, 0.3);
  }

  /* Projects Section - Red */
  .nav-projects {
    border-color: rgba(226, 85, 85, 0.3);
  }

  .nav-projects .nav-brand {
    color: #E25555;
  }

  .nav-projects .nav-brand:hover {
    color: #ff6b6b;
  }

  .nav-projects .nav-toggle {
    color: rgba(226, 85, 85, 0.7);
  }

  .nav-projects .nav-toggle:hover {
    color: #E25555;
  }

  .nav-projects .nav-toggle.active {
    color: #E25555;
  }

  .nav-projects .nav-toggle.active .toggle-indicator {
    left: calc(100% - 0.875rem - 0.1875rem);
    background: #E25555;
    box-shadow: 0 0 8px rgba(226, 85, 85, 0.8);
  }

  /* Contact Section - Green Toggle */
  .nav-contact .toggle-switch {
    background: rgba(18, 39, 35, 0.8);
    border: 1px solid rgba(31, 90, 71, 0.5);
  }

  .nav-contact .toggle-indicator {
    background: rgba(78, 214, 166, 0.6);
  }

  .nav-projects .hamburger-line {
    background: #E25555;
  }

  .nav-projects .mobile-menu-title {
    color: #E25555;
  }

  .nav-projects .mobile-menu-drawer {
    border-left-color: rgba(226, 85, 85, 0.3);
  }

  /* Contact Section - Green */
  .nav-contact {
    border-color: rgba(78, 214, 166, 0.3);
  }

  .nav-contact .nav-brand {
    color: #4ED6A6;
  }

  .nav-contact .nav-brand:hover {
    color: #7FF2C4;
  }

  .nav-contact .nav-toggle {
    color: rgba(78, 214, 166, 0.7);
  }

  .nav-contact .nav-toggle:hover {
    color: #4ED6A6;
  }

  .nav-contact .nav-toggle.active {
    color: #4ED6A6;
  }

  .nav-contact .nav-toggle.active .toggle-indicator {
    left: calc(100% - 0.875rem - 0.1875rem);
    background: #4ED6A6;
    box-shadow: 0 0 8px rgba(78, 214, 166, 0.8);
  }

  .nav-contact .hamburger-line {
    background: #4ED6A6;
  }

  .nav-contact .mobile-menu-title {
    color: #4ED6A6;
  }

  .nav-contact .mobile-menu-drawer {
    border-left-color: rgba(78, 214, 166, 0.3);
  }

  /* Mobile menu active states for each section */
  .nav-hero .mobile-nav-item.active {
    color: #ff8c00;
    background: rgba(255, 140, 0, 0.15);
  }

  .nav-hero .mobile-nav-item.active .mobile-toggle-indicator {
    left: calc(100% - 0.75rem - 0.125rem);
    background: #ff8c00;
    box-shadow: 0 0 8px rgba(255, 140, 0, 0.8);
  }

  .nav-experience .mobile-nav-item.active {
    color: #4a9eff;
    background: rgba(74, 158, 255, 0.15);
  }

  .nav-experience .mobile-nav-item.active .mobile-toggle-indicator {
    left: calc(100% - 0.75rem - 0.125rem);
    background: #4a9eff;
    box-shadow: 0 0 8px rgba(74, 158, 255, 0.8);
  }

  .nav-projects .mobile-nav-item.active {
    color: #E25555;
    background: rgba(226, 85, 85, 0.15);
  }

  .nav-projects .mobile-nav-item.active .mobile-toggle-indicator {
    left: calc(100% - 0.75rem - 0.125rem);
    background: #E25555;
    box-shadow: 0 0 8px rgba(226, 85, 85, 0.8);
  }

  .nav-contact .mobile-nav-item.active {
    color: #4ED6A6;
    background: rgba(78, 214, 166, 0.15);
  }

  .nav-contact .mobile-nav-item.active .mobile-toggle-indicator {
    left: calc(100% - 0.75rem - 0.125rem);
    background: #4ED6A6;
    box-shadow: 0 0 8px rgba(78, 214, 166, 0.8);
  }

  /* Desktop Navigation */
  .desktop-nav {
    display: flex;
  }

  .nav-toggle {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    text-decoration: none;
    transition: all 0.3s ease;
    position: relative;
    font-size: 0.875rem;
    font-weight: 500;
  }

  /* Default colors handled by section classes */

  .toggle-switch {
    width: 2.5rem;
    height: 1.25rem;
    border-radius: 0.625rem;
    position: relative;
    box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.5);
    transition: background 0.4s ease, border-color 0.4s ease;
  }

  .toggle-indicator {
    width: 0.875rem;
    height: 0.875rem;
    border-radius: 50%;
    position: absolute;
    left: 0.1875rem;
    top: 50%;
    transform: translateY(-50%);
    transition: all 0.3s ease;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.5);
  }

  /* Hero Section - Gold/Orange Toggle */
  .nav-hero .toggle-switch {
    background: #2a2520;
    border: 1px solid #8b4513;
  }

  .nav-hero .toggle-indicator {
    background: #8b4513;
  }

  /* Active state handled by section classes */

  .toggle-label {
    font-family: 'Courier New', monospace;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Mobile Hamburger Button */
  .mobile-menu-btn {
    display: none;
    flex-direction: column;
    justify-content: space-around;
    width: 2rem;
    height: 2rem;
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 0;
    z-index: 51;
    position: relative;
  }

  .hamburger-line {
    width: 100%;
    height: 3px;
    border-radius: 2px;
    transition: all 0.3s ease;
    transform-origin: center;
  }

  .mobile-menu-btn[aria-expanded="true"] .hamburger-line:nth-child(1) {
    transform: rotate(45deg) translate(8px, 8px);
  }

  .mobile-menu-btn[aria-expanded="true"] .hamburger-line:nth-child(2) {
    opacity: 0;
  }

  .mobile-menu-btn[aria-expanded="true"] .hamburger-line:nth-child(3) {
    transform: rotate(-45deg) translate(7px, -7px);
  }

  /* Mobile Side Menu */
  .mobile-menu {
    display: none;
  }

  .mobile-menu-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(4px);
    z-index: 9998;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
  }

  .mobile-menu.active .mobile-menu-overlay {
    opacity: 1;
    visibility: visible;
  }

  .mobile-menu-drawer {
    position: fixed;
    top: 0;
    right: -100%;
    width: 280px;
    max-width: 85vw;
    height: 100vh;
    background: linear-gradient(180deg, #1a1612 0%, #0f0d0a 100%);
    border-left: 2px solid;
    z-index: 9999;
    transition: right 0.3s ease, border-color 0.4s ease;
    overflow-y: auto;
    box-shadow: -4px 0 20px rgba(0, 0, 0, 0.5);
  }

  .mobile-menu.active .mobile-menu-drawer {
    right: 0;
  }

  .mobile-menu-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem;
    border-bottom: 1px solid;
    transition: border-color 0.4s ease;
  }

  .nav-hero .mobile-menu-header {
    border-bottom-color: rgba(255, 140, 0, 0.3);
  }

  .nav-experience .mobile-menu-header {
    border-bottom-color: rgba(74, 158, 255, 0.3);
  }

  .nav-projects .mobile-menu-header {
    border-bottom-color: rgba(226, 85, 85, 0.3);
  }

  .nav-contact .mobile-menu-header {
    border-bottom-color: rgba(78, 214, 166, 0.3);
  }

  .mobile-menu-title {
    font-size: 1rem;
    font-weight: bold;
  }

  .mobile-menu-close {
    background: transparent;
    border: none;
    font-size: 2rem;
    line-height: 1;
    cursor: pointer;
    padding: 0;
    width: 2rem;
    height: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.2s ease, color 0.4s ease;
  }

  .nav-hero .mobile-menu-close {
    color: #ff8c00;
  }

  .nav-experience .mobile-menu-close {
    color: #4a9eff;
  }

  .nav-projects .mobile-menu-close {
    color: #E25555;
  }

  .nav-contact .mobile-menu-close {
    color: #4ED6A6;
  }

  .mobile-menu-close:hover {
    transform: scale(1.1);
  }

  .close-icon {
    display: block;
    font-weight: 300;
  }

  .mobile-menu-content {
    padding: 1rem 0;
  }

  .mobile-nav-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 1.5rem;
    text-decoration: none;
    transition: all 0.3s ease;
    border-bottom: 1px solid;
  }

  /* Mobile nav default colors by section */
  .nav-hero .mobile-nav-item {
    color: #d4a574;
    border-bottom-color: rgba(255, 140, 0, 0.1);
  }

  .nav-hero .mobile-nav-item:hover {
    background: rgba(255, 140, 0, 0.1);
    color: #ff8c00;
  }

  .nav-experience .mobile-nav-item {
    color: rgba(74, 158, 255, 0.7);
    border-bottom-color: rgba(74, 158, 255, 0.1);
  }

  .nav-experience .mobile-nav-item:hover {
    background: rgba(74, 158, 255, 0.1);
    color: #4a9eff;
  }

  .nav-projects .mobile-nav-item {
    color: rgba(226, 85, 85, 0.7);
    border-bottom-color: rgba(226, 85, 85, 0.1);
  }

  .nav-projects .mobile-nav-item:hover {
    background: rgba(226, 85, 85, 0.1);
    color: #E25555;
  }

  .nav-contact .mobile-nav-item {
    color: rgba(78, 214, 166, 0.7);
    border-bottom-color: rgba(78, 214, 166, 0.1);
  }

  .nav-contact .mobile-nav-item:hover {
    background: rgba(78, 214, 166, 0.1);
    color: #4ED6A6;
  }

  /* Mobile nav active states handled by section classes */

  .mobile-nav-label {
    font-family: 'Courier New', monospace;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-size: 0.875rem;
    font-weight: 500;
  }

  .mobile-toggle-switch {
    width: 2rem;
    height: 1rem;
    border-radius: 0.5rem;
    position: relative;
    box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.5);
    transition: background 0.4s ease, border-color 0.4s ease;
  }

  .mobile-toggle-indicator {
    width: 0.75rem;
    height: 0.75rem;
    border-radius: 50%;
    position: absolute;
    left: 0.125rem;
    top: 50%;
    transform: translateY(-50%);
    transition: all 0.3s ease;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.5);
  }

  /* Mobile toggle colors by section */
  .nav-hero .mobile-toggle-switch {
    background: #2a2520;
    border: 1px solid #8b4513;
  }

  .nav-hero .mobile-toggle-indicator {
    background: #8b4513;
  }

  .nav-experience .mobile-toggle-switch {
    background: rgba(10, 10, 26, 0.8);
    border: 1px solid rgba(74, 158, 255, 0.4);
  }

  .nav-experience .mobile-toggle-indicator {
    background: rgba(74, 158, 255, 0.6);
  }

  .nav-projects .mobile-toggle-switch {
    background: rgba(42, 23, 23, 0.8);
    border: 1px solid rgba(122, 43, 43, 0.5);
  }

  .nav-projects .mobile-toggle-indicator {
    background: rgba(226, 85, 85, 0.6);
  }

  .nav-contact .mobile-toggle-switch {
    background: rgba(18, 39, 35, 0.8);
    border: 1px solid rgba(31, 90, 71, 0.5);
  }

  .nav-contact .mobile-toggle-indicator {
    background: rgba(78, 214, 166, 0.6);
  }

  /* Responsive Styles */
  @media (max-width: 768px) {
    .desktop-nav {
      display: none;
    }

    .mobile-menu-btn {
      display: flex;
    }

    .mobile-menu {
      display: block;
    }

    nav {
      padding: 0.5rem 0;
    }

    nav a:first-child {
      font-size: 0.875rem;
    }

    nav a:first-child span {
      font-size: 0.875rem;
    }
  }

  @media (max-width: 480px) {
    nav {
      padding: 0.375rem 0;
    }

    nav a:first-child {
      font-size: 0.75rem;
    }

    nav a:first-child span {
      font-size: 0.75rem;
    }

    .mobile-menu-drawer {
      width: 260px;
    }

    .mobile-menu-header {
      padding: 1.25rem;
    }

    .mobile-nav-item {
      padding: 0.875rem 1.25rem;
    }
  }
</style>

<script is:inline>
  // Scroll-based nav hide/show with rubber band animation
  (function() {
    let lastScrollY = 0;
    let ticking = false;
    let hasScrolled = false; // Track if user has scrolled down
    const scrollThreshold = 50; // Hide nav when scrolled less than 50px from top

    function updateNavVisibility() {
      const nav = document.getElementById('main-nav');
      if (!nav) {
        ticking = false;
        return;
      }

      const currentScrollY = window.scrollY;
      
      console.log('Scroll position:', currentScrollY, 'hasScrolled:', hasScrolled);

      // Check if we've scrolled down significantly
      if (currentScrollY > scrollThreshold) {
        hasScrolled = true;
      }

      const isAtTop = currentScrollY < scrollThreshold;
      const currentlyHidden = nav.classList.contains('hidden');

      if (isAtTop && hasScrolled) {
        // At top of page AND user has scrolled before - hide nav
        if (!currentlyHidden) {
          console.log('Hiding nav');
          nav.classList.add('hidden');
          nav.classList.remove('showing');
        }
      } else if (!isAtTop) {
        // Scrolled down - show nav with slide animation
        if (currentlyHidden) {
          console.log('Showing nav');
          // Force a reflow to ensure the transition is triggered
          nav.offsetHeight;
          nav.classList.remove('hidden');
        }
      }

      lastScrollY = currentScrollY;
      ticking = false;
    }

    function onScroll() {
      if (!ticking) {
        window.requestAnimationFrame(updateNavVisibility);
        ticking = true;
      }
    }

    // Wait for DOM to be ready
    function initNavScroll() {
      const nav = document.getElementById('main-nav');
      
      if (!nav) {
        console.log('Nav element not found, retrying...');
        setTimeout(initNavScroll, 100);
        return;
      }

      console.log('Nav initialized, starting visible');
      
      // Ensure nav starts visible
      nav.classList.remove('hidden');
      nav.style.transform = 'translateY(0)';

      // Add scroll listener
      window.addEventListener('scroll', onScroll, { passive: true });
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initNavScroll);
    } else {
      initNavScroll();
    }
  })();

  // Mobile menu toggle
  (function() {
    function initMobileMenu() {
      const menuToggle = document.getElementById('mobile-menu-toggle');
      const menuClose = document.getElementById('mobile-menu-close');
      const mobileMenu = document.getElementById('mobile-menu');
      const menuOverlay = mobileMenu?.querySelector('.mobile-menu-overlay');
      const menuLinks = mobileMenu?.querySelectorAll('.mobile-nav-item');

      if (!menuToggle || !mobileMenu) return;

      function openMenu() {
        mobileMenu.classList.add('active');
        menuToggle.setAttribute('aria-expanded', 'true');
        document.body.style.overflow = 'hidden';
      }

      function closeMenu() {
        mobileMenu.classList.remove('active');
        menuToggle.setAttribute('aria-expanded', 'false');
        document.body.style.overflow = '';
      }

      // Toggle button
      menuToggle.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        if (mobileMenu.classList.contains('active')) {
          closeMenu();
        } else {
          openMenu();
        }
      });

      // Close button
      if (menuClose) {
        menuClose.addEventListener('click', function(e) {
          e.preventDefault();
          e.stopPropagation();
          closeMenu();
        });
      }

      // Overlay click to close
      if (menuOverlay) {
        menuOverlay.addEventListener('click', function(e) {
          e.preventDefault();
          e.stopPropagation();
          closeMenu();
        });
      }

      // Close menu when clicking nav links
      if (menuLinks) {
        menuLinks.forEach(function(link) {
          link.addEventListener('click', function() {
            // Small delay to allow smooth scroll
            setTimeout(closeMenu, 300);
          });
        });
      }

      // Close menu on escape key
      document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && mobileMenu.classList.contains('active')) {
          closeMenu();
        }
      });
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initMobileMenu);
    } else {
      setTimeout(initMobileMenu, 100);
    }
  })();
</script>

